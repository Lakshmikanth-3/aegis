{
  "name": "aegis-orchestrator",
  "version": "1.0.0",
  "description": "Aegis agent-payment orchestrator: real Poseidon commitments (verified bit-identical to the Noir circuit's), real Noir/UltraHonk proof generation via nargo+bb (WSL), real submission to the deployed Stellar testnet contract via stellar-cli (WSL). No mocked proof bytes, no mocked transaction hashes.",
  "main": "src/server.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "seed": "tsx src/seed.ts",
    "demo": "tsx src/demo-run.ts",
    "selftest": "tsx src/selftest.ts",
    "build": "tsc"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "poseidon-lite": "^0.3.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
